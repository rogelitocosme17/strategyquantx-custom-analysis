# 🧠 StrategyQuant X Custom Analysis Module

A custom Java analysis module for **StrategyQuant X**, designed to extend the platform’s analytics capabilities by adding new performance metrics, visual reports, or advanced statistical evaluations.

---

## 📌 Overview

This project implements a **Custom Analysis Plugin** for [StrategyQuant X](https://strategyquant.com/), allowing users to perform extended post-backtest evaluations of generated trading strategies.

**Main Features:**
- [✅ Add your feature here, e.g. “Calculates a new risk-adjusted return ratio.”]
- [✅ Add another feature, e.g. “Provides additional metrics beyond default SQX performance tables.”]
- [✅ Optional visualization or export functionality.]

---

## 🏗️ Architecture

The custom analysis extends the built-in SQX framework by implementing the following key interfaces:

```java
import com.strategyquant.lib.*;
import com.strategyquant.tradinglib.*;
import com.strategyquant.tradinglib.performance.custom.*;
